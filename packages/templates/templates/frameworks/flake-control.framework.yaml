id: flake-control
name: Flake control policy
owner_agent: test-architect
produces: { adr_category: testing, kb_section: engineering/testing.md }
inputs:
  required: [kb:constraints/**]
# 13 §13.1's own F-TEST-6 is a fixed policy (retry-once-to-classify, quarantine above 2%/20 runs,
# quarantine cap 5, retries never used to make a gate pass), not a choice among named alternatives.
# One option, no criteria/rules, the same "execution step, not a decision" shape already established.
options:
  - id: adopt-flake-quarantine-policy
scoring: rules
output_template: templates/adr-flake-control.md.hbs
follow_on:
  - create_stories_from: templates/stories/flaky-report-wiring.yaml
