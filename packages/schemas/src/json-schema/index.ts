/**
 * `@forge/schemas/json-schema` — a committed, byte-stable JSON Schema per artifact type and config.
 *
 * @see specs/02 §2.1
 */
export { ARTIFACT_SCHEMAS, emitJsonSchemas, type SchemaFileName } from './emit.ts';
